<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ship.web.lol.LolMapper">
	<select id="createLolTable" parameterType="hashmap">
		${CREATE_LOL}
	</select>
	
</mapper>