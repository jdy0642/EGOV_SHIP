<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ship.web.report.ReportMapper">
	<select id="createReportTalbe" parameterType="hashmap">
		${CREATE_REPORT}
	</select>
	
</mapper>