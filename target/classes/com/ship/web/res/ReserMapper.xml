<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ship.web.res.ReserMapper">

	<select id="createRes" parameterType="hashmap">
		${CREATE_RES}
	</select>
	<insert id="insertReser" parameterType="Reser">
		INSERT INTO RESERVATION (RESNUM, RESDATE, USERPOINT,
		RESCAN,PAYNUM,PAYAMOUNT,PAYWAY,PAYCAN,ADMINID) VALUES (
		#{resnum}, #{resdate}, #{userponint}, #{rescan},
		#{paynum},#{payamount},#{payway},#{paycan},#{adminid})
	</insert>
	<select id="countReser" resultType="string">
		SELECT COUNT(*) FROM RESERVATION
	</select>
</mapper>